# CTI-OS v9 neural temporal agent (pre-reg)

This artifact introduces a **small neural temporal adapter** inside the
existing deterministic verification system.

Allowed claim: a neural temporal model is tested against scalar + naive
baselines on hidden regime-shift adaptation.

Forbidden claim: intelligence, cognition, AGI, brain simulation, or
biological time perception.
