# Hidden parameters. Agents NEVER receive these — only the runner/oracle.
tau0: 10.0      # pre-shift mean interval
tau1: 17.0      # post-shift mean interval (hidden regime change)
t_star: 300     # step at which the regime shifts
sigma: 1.0      # observation noise on each realised interval
n_steps: 600    # total steps per episode
