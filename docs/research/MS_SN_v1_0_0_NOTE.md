# MS-SN v1.0.0 research scaffold specification

Status: research-only deterministic scaffold.

Claim boundary:

`research-only deterministic scaffold; runtime absent; no production, biological, intelligence, or empirical validation claim`

This document defines only scaffold-level protocol artifacts:
- preregistered configuration lock,
- scaffold evidence schema and validation,
- runtime-absent contract,
- deterministic CI gate behavior.

Runtime implementation is intentionally absent in PR #74 and is out of scope.
