# CONVERGENCE — v6 + v7 negatives, one boundary (pinned)

Two surface-distinct REDs (v6 precision-weighting, v7 reservoir/SSM) converge: on this rupture class the scalar heuristic is within 14.1% of the oracle floor (agg 0.9285 vs 0.7971). No model class can pay its complexity here — there is no headroom. This is a delivered boundary artifact, not a string of failures: the v7 environment must change (provable scalar-inexpressible structure) before any stronger-model question is scientifically askable. Frozen v4 untouched.
