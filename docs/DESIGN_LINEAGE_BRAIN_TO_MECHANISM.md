# Design Lineage — Brain Motivation → Repo Mechanism

Generated by `python -m ctios.design_lineage` (fails if any cited mechanism/test is missing — this is a guard, not prose).

**Boundary.** NOT a brain, neurons, cognition, consciousness, sentience, intelligence, AGI, neuroplasticity, or a theory of time. The seven facts are design lineage (motivation) only; every claim is scoped to an executable mechanism + its test.

| # | brain fact (motivation) | engineering invariant | mechanism | test | not claimed |
|---|---|---|---|---|---|
| 1 | Thinking is prediction, not reaction — the system is moved by its own prediction error (predictive-coding motivation). | An agent infers a hidden regime from its OWN error, never from an exogenous change signal. | `src/ctios/predictive_simulation.py` | `tests/test_predictive_simulation.py` | NOT Friston/biological predictive coding; a scalar/windowed estimator on synthetic streams. |
| 2 | Conscious decision lags the act (Libet) — the self is editor, not author. | The verdict is decided by a falsifier PINNED before the run; reports edit a sealed result, they never author it (verdict-isolation). | `src/ctios/falsify.py` | `tests/test_falsify_engine.py` | NOT a claim about consciousness or volition. |
| 3 | Attention is a gate, not a spotlight — it blocks the rest. | The battery REJECTS inadmissible/degenerate probes and the lexicon BLOCKS unscoped surface; admission is by exclusion. | `src/ctios/falsifier_battery.py` | `tests/test_deep_adversarial_probes.py` | NOT a model of attention. |
| 4 | Working memory is a 7±2 bottleneck; chunking removes the limit. | A private theorem is chunked into a fixed small redacted skeleton; 6 lineages distilled to one primitive. | `src/ctios/change_detection.py` | `tests/test_change_detection.py` | NOT a memory model. |
| 5 | Emotion is part of logic (Damasio) — pure logic with no value signal cannot decide. | Automated logic cannot self-authorize the next experiment; a value-bearing human decision is structurally required. | `src/ctios/human_gate.py` | `tests/test_human_gate.py` | NOT a model of emotion or affect. |
| 6 | Metacognition interferes with automatic performance — over-reflection is interference, not control. | The science verdict is NOT asserted in CI; meta-monitoring (CI green) must not govern the object-level result. | `src/ctios/external_validation.py` | `tests/test_external_validation_tamper.py` | NOT a claim about metacognition. |
| 7 | The brain does not separate 'I think' from 'I do' — a thought is a prototype of action (motor simulation). | A hypothesis is not prose: it is a runnable Probe. Thinking the claim and doing the experiment are one artifact. | `src/ctios/spec_compiler.py` | `tests/test_spec_compiler.py` | NOT mirror neurons or motor cortex. |

all_mechanisms_present: **True**
missing: (none)

If a row cannot point at a real, tested mechanism it is deleted, not softened. Inspiration without a mechanism is not stated here.
